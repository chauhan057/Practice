*------------- Array- it is a collection of homogenious data type element
it has fixed length,
ex -:
    var nums =arrayOf(1,2,3)



* -------------------List - it provide dynamic behaviour ,in list we can modify,delete and insert the data.
ex-:
    var nums =listOf(1,2,3)

types of list -

1. Mutable list         (Can be changed i.e. element can be added/removed/replaced)
    ex-: var arr = listOf(1,2,3)
2. ImmutableList        ( Can't be changed)
    ex-: var arr = mutableListOf(1,2,3)
        a. In array we can modify data by index value,
            ex-: In Array-:
                    var arr= arrayOf(1,2,3)
                    arr[0]=2  (right)

        b. but in list ,we can't modify the data,
            ex-: InImmutableList-:
                var arr = listOf(1,2,3)
                arr[0]=2 (wrong)
*----------Maps------
. it store the data in key value paires

types of map-

1.mutable

2.immutable